{
  "name": "buzzme",
  "version": "1.0.0",
  "description": "BuzzMe — Silent buzz app for couples. Monorepo root.",
  "private": true,
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "test:server": "npm run test --workspace=server"
  }
}
