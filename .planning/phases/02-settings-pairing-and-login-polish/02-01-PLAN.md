---
wave: 1
depends_on: []
files_modified:
  - mobile/src/screens/SettingsScreen.tsx
  - mobile/src/navigation/TabNavigator.tsx
autonomous: true
requirements_addressed: [REQ-002]
---

# Plan 02-01: Implement Settings Screen

## Objective
Create the Settings screen exactly as specified in the Phase 2 roadmap. Implement partner connection section, vibration pattern selector, intensity slider, and connection toggles.

## Tasks

### 1. Build SettingsScreen
<read_first>
- mobile/src/screens/BuzzScreen.tsx (for theme and exact scroll/layout styling)
- .planning/ROADMAP.md
</read_first>

<action>
Create `mobile/src/screens/SettingsScreen.tsx`. Use mock data: partner "Sarah", connected status "true", 70% vibration intensity default. Implement layout with `GeistPixel` font and components:
- Header: Top "SETTINGS" label.
- Section 1: Partner (connection status).
- Section 2: Buzz intensity slider or mock visual (e.g., standard text/view UI matching 70%).
- Section 3: Vibration patterns (Short, Pulse, Heartbeat).
</action>

<acceptance_criteria>
- `mobile/src/screens/SettingsScreen.tsx` exports default function
- File uses `GeistPixel` font family
- Contains string "Sarah" for partner
- Contains "70%" or equivalent for intensity 
- No UI blocking compilation errors
</acceptance_criteria>

### 2. Wire Settings Tab
<read_first>
- mobile/src/navigation/TabNavigator.tsx
- mobile/src/components/TabBar.tsx
</read_first>

<action>
In `mobile/src/navigation/TabNavigator.tsx`, replace `SettingsPlaceholder` with the new `SettingsScreen`.
Import `SettingsScreen` from `../screens/SettingsScreen`.
Update the `renderScreen` switch block to return `<SettingsScreen />` for the `'Settings'` case.
</action>

<acceptance_criteria>
- `mobile/src/navigation/TabNavigator.tsx` imports `SettingsScreen`
- `TabNavigator` renders `<SettingsScreen />` for active tab `'Settings'`
</acceptance_criteria>

## Verification
- Settings tab in bottom navigator takes the user to `SettingsScreen`
- Valid layout and typography (`GeistPixel` applied everywhere)
- Must haves: Uses existing theme (`Colors`, `Typography` constants)
