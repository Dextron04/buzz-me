---
wave: 2
depends_on: [02-01]
files_modified:
  - mobile/src/screens/PairingScreen.tsx
  - mobile/src/navigation/TabNavigator.tsx
  - mobile/src/navigation/AppNavigator.tsx
autonomous: true
requirements_addressed: [REQ-003]
---

# Plan 02-02: Implement Pairing Screen

## Objective
Create the Pairing Screen as specified in the Phase 2 roadmap, showing a connect interface ("Your Code" and "Enter Partner's Code") accessible from the Settings Screen.

## Tasks

### 1. Build PairingScreen
<read_first>
- mobile/src/screens/LoginScreen.tsx (for Field/TextInput components)
- .planning/ROADMAP.md
</read_first>

<action>
Create `mobile/src/screens/PairingScreen.tsx`. Layout must use `GeistPixel` everywhere, and import `Colors`, `Spacing`, etc. from `src/theme/index.ts`.
- Top Header: back button and Title "PAIRING".
- Display: "Your Code" section with large mock text `BUZZ-7X3K` and a visual "Share" button.
- Input: "Enter Partner's Code" using an existing `Field` component or custom text input.
- Button: Large "CONNECT" button at bottom.
</action>

<acceptance_criteria>
- `mobile/src/screens/PairingScreen.tsx` exports default function
- Uses `GeistPixel` font
- Contains `BUZZ-7X3K`
- Input field styled appropriately
</acceptance_criteria>

### 2. Wire Navigation to Pairing
<read_first>
- mobile/src/navigation/AppNavigator.tsx
- mobile/src/screens/SettingsScreen.tsx
</read_first>

<action>
Modify `mobile/src/navigation/AppNavigator.tsx` to include `PairingScreen` in `RootStackParamList` as `Pair`.
Add `Stack.Screen name="Pair" component={PairingScreen}` in `AppNavigator.tsx`.
In `mobile/src/screens/SettingsScreen.tsx`, add navigation to `Pair` when standard "Pair New Partner" button/area is pressed using `useNavigation`.
</action>

<acceptance_criteria>
- `AppNavigator.tsx` imports `PairingScreen`
- `SettingsScreen` invokes `navigation.navigate('Pair')` mock logic
</acceptance_criteria>

## Verification
- Can route settings page to Pairing page
- Must haves: UI matches neon scheme and pixel aesthetics accurately
