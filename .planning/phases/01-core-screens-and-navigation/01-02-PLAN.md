---
wave: 1
depends_on: []
files_modified:
  - mobile/src/screens/BuzzScreen.tsx
requirements_addressed: [REQ-005]
autonomous: true
---

# Plan 01-02: BuzzScreen — Home Screen Design Polish

## Objective

Update `BuzzScreen.tsx` to match the Stitch "BuzzMe Futuristic Home Screen" design. Add a partner header section with online status indicator and a "Today's Buzzes" stats badge, aligning with the Stitch comp while keeping all existing animation logic intact.

## Stitch Design Reference (Screen: 685fa4a255b941159fb6938015fdd171)

The Stitch Home Screen shows:
- Top: App name "BuzzMe" small header
- Connected status indicator (green dot + "Connected")
- Partner name large ("Sarah") with "Online" / "favorite" label
- "Today's Buzzes" stat row: "12 Sent · 8 Recv"
- Center: Large BUZZ button with ripple rings
- Bottom: Pill nav (HOME · HISTORY · SETTINGS)

Design aesthetic: Dark background (`#0B0B0B`), neon yellow accent (`#E1FF01`), `GeistPixel` font, sharp pixel borders on accent elements.

## Tasks

### Task 1: Add partner header section to BuzzScreen

<read_first>
- mobile/src/screens/BuzzScreen.tsx (read the full file: find where the main content begins, the existing header/title, and where the buzz button sits)
- mobile/src/theme/index.ts (Colors.accent, Colors.background, Colors.text, Colors.textSecondary)
</read_first>

<action>
In `mobile/src/screens/BuzzScreen.tsx`, add a header section ABOVE the buzz button area. This header should display:

1. **App name row** — "BuzzMe" in small caps / pixel font, centered or left-aligned
2. **Connection status** — a green circle (8×8, `#00FF88`) + text "Connected" in `GeistPixel`, font size 12
3. **Partner name** — `MOCK_USER.partner` in large text (font size 28), `GeistPixel`, color `Colors.accent` (`#E1FF01`)
4. **Partner status** — "Online" in small text, color `#00FF88` (green)
5. **Stats badge** — A row showing "12 Sent · 8 Recv" (use `MOCK_USER.todaySent` and `MOCK_USER.todayReceived`); font size 13, color `Colors.textSecondary` or `'#888888'`; separated by a thin vertical divider or the "·" character

Implement this as a new `<View>` block inside the existing screen layout. Place it near the top of the main content (before the buzz button), respecting the existing padding/margin pattern.

Style guidelines:
- Container: `paddingHorizontal: 24`, `paddingTop: 16`, `alignItems: 'center'`
- Partner name: `fontFamily: Typography.pixel`, `fontSize: 28`, `color: Colors.accent`, `letterSpacing: 2`
- Status: `fontFamily: Typography.pixel`, `fontSize: 12`, `color: '#00FF88'`
- Stats: `fontFamily: Typography.pixel`, `fontSize: 13`, `color: '#888888'`
</action>

<acceptance_criteria>
- `mobile/src/screens/BuzzScreen.tsx` contains text rendering `MOCK_USER.partner`
- `mobile/src/screens/BuzzScreen.tsx` contains `MOCK_USER.todaySent` and `MOCK_USER.todayReceived`
- `mobile/src/screens/BuzzScreen.tsx` contains `color: Colors.accent` on the partner name text
- `mobile/src/screens/BuzzScreen.tsx` contains `'#00FF88'` (green status color)
- `mobile/src/screens/BuzzScreen.tsx` contains `'Connected'` status text
</acceptance_criteria>

---

### Task 2: Ensure BUZZ button uses accent color correctly

<read_first>
- mobile/src/screens/BuzzScreen.tsx (find the BUZZ button element and its current styles)
</read_first>

<action>
Verify the BUZZ button (the circular tap target in the center of the screen):
1. The button border/ring color must be `Colors.accent` (`#E1FF01`)
2. The "BUZZ" label text inside must use `fontFamily: Typography.pixel` (GeistPixel)
3. The ripple rings that animate outward on press must use `Colors.accent` with reduced opacity (e.g., `rgba(225, 255, 1, 0.3)`)

If any of these are already correct, do not change them. Only fix what does not match. The animation logic itself must NOT be touched.
</action>

<acceptance_criteria>
- `mobile/src/screens/BuzzScreen.tsx` contains `Colors.accent` applied to the buzz button border or ring color
- The BUZZ button label uses GeistPixel (Typography.pixel)
</acceptance_criteria>

## Verification

```bash
# Partner data references exist
grep -n "MOCK_USER.partner\|MOCK_USER.todaySent\|MOCK_USER.todayReceived" mobile/src/screens/BuzzScreen.tsx

# Connected status
grep -n "Connected" mobile/src/screens/BuzzScreen.tsx

# Accent color on partner name area
grep -n "Colors.accent" mobile/src/screens/BuzzScreen.tsx
```

## Must-Haves

- Partner header section visible in the screen layout
- Partner name drawn from `MOCK_USER.partner`
- Today's stats shown using `MOCK_USER.todaySent` and `MOCK_USER.todayReceived`
- Connection status indicator present
- All existing animation logic preserved (do not remove or break `Animated` values, ring animations, haptics)
