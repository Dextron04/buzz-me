---
wave: 3
depends_on: [02-02]
files_modified:
  - mobile/src/screens/LoginScreen.tsx
autonomous: true
requirements_addressed: [REQ-007]
---

# Plan 02-03: Login & Welcome Polish

## Objective
Refine the LoginScreen visual design to ensure full design fidelity to the Stitch welcome screen. Make entrance animation timings snappier, enforce the `GeistPixel` font, and ensure correct copy mapping.

## Tasks

### 1. Refine LoginScreen Component
<read_first>
- mobile/src/screens/LoginScreen.tsx
- mobile/src/components/LoginForm.tsx
- mobile/src/components/RegisterForm.tsx
</read_first>

<action>
Review `mobile/src/screens/LoginScreen.tsx` and the form components:
- Ensure all explicit text (`"BUZZME"`, `"Welcome back"`, `"Don't have an account?"`) uses `GeistPixel` font token and appropriate letter spacing.
- Tighten the entrance animation timing (e.g., lower `duration` in `Animated.timing` by 20-30% for a snappier slide-up context). 
- Ensure button sizes, field spacing, and background elements align with `Colors.surface` instead of hard-coded colors.
- Polish pixel ratios for responsive layout since we previously brought `SCREEN_H` changes in.
</action>

<acceptance_criteria>
- No `#161616` hardcoded values in `LoginScreen.tsx`
- All animations use adjusted timings
- All forms use the `GeistPixel` typography styling correctly
</acceptance_criteria>

## Verification
- Login/Register panel animations slide-up rapidly
- Typography exactly maps pixel aesthetics
- Reboots compilation check passed
