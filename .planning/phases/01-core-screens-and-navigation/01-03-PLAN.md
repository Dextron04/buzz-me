---
wave: 1
depends_on: []
files_modified:
  - mobile/src/screens/HistoryScreen.tsx
requirements_addressed: [REQ-001]
autonomous: true
---

# Plan 01-03: History Screen

## Objective

Create `mobile/src/screens/HistoryScreen.tsx` — a scrollable buzz history log that matches the Stitch "BuzzMe Futuristic History Screen" design. This is a fully new file; no existing screen to modify.

## Stitch Design Reference (Screen: 77dd1c30483e487bb4784aa77b80a99e)

The Stitch History Screen shows:
- Header: "History" in `GeistPixel`
- Section header: "This Week" + count badge "42 buzzes 💛"
- List of buzz entries:
  - Each row: direction label (sent/received) + partner name + relative timestamp
  - Example rows:
    - "You buzzed Sarah · 2 min ago"
    - "Sarah buzzed you · 15 min ago"
    - "You buzzed Sarah · 1 hour ago"
    - "Sarah buzzed you · 3 hours ago"
    - "You buzzed Sarah · Yesterday"
- Bottom pill nav (Home / History / Settings), History tab active
- Dark background, neon accent for sent buzzes, muted color for received

## Mock Data

```typescript
const MOCK_HISTORY = [
    { id: '1', direction: 'sent', timestamp: '2 min ago' },
    { id: '2', direction: 'received', timestamp: '15 min ago' },
    { id: '3', direction: 'sent', timestamp: '1 hour ago' },
    { id: '4', direction: 'received', timestamp: '3 hours ago' },
    { id: '5', direction: 'sent', timestamp: 'Yesterday' },
    { id: '6', direction: 'received', timestamp: 'Yesterday' },
    { id: '7', direction: 'sent', timestamp: '2 days ago' },
    { id: '8', direction: 'received', timestamp: '2 days ago' },
    { id: '9', direction: 'sent', timestamp: '3 days ago' },
    { id: '10', direction: 'received', timestamp: '4 days ago' },
];
const MOCK_PARTNER = 'Sarah';
const MOCK_WEEK_COUNT = 42;
```

## Tasks

### Task 1: Create `HistoryScreen.tsx`

<read_first>
- mobile/src/screens/BuzzScreen.tsx (reference for screen structure, bottom nav pill pattern, Colors/Typography usage)
- mobile/src/theme/index.ts (all available Colors, Typography, Spacing, Radius tokens)
- mobile/src/navigation/AppNavigator.tsx (understand current navigation types for future wiring)
</read_first>

<action>
Create `mobile/src/screens/HistoryScreen.tsx` with the following structure:

**File structure:**
```typescript
import React, { useEffect, useRef } from 'react';
import {
    View, Text, ScrollView, StyleSheet, Animated, SafeAreaView
} from 'react-native';
import { Colors, Typography, Spacing } from '../theme';
import { getResponsiveValue } from '../utils/responsive';

// Mock data (defined at module level, before component)
const MOCK_HISTORY = [ /* 10 entries as specified above */ ];
const MOCK_PARTNER = 'Sarah';
const MOCK_WEEK_COUNT = 42;

interface HistoryScreenProps {
    onNavigate?: (screen: 'Home' | 'History' | 'Settings') => void;
    activeTab?: 'Home' | 'History' | 'Settings';
}

export default function HistoryScreen({ onNavigate, activeTab = 'History' }: HistoryScreenProps) {
    // Entry animation
    const headerOpacity = useRef(new Animated.Value(0)).current;
    const listOpacity = useRef(new Animated.Value(0)).current;
    const listTranslateY = useRef(new Animated.Value(20)).current;

    useEffect(() => {
        Animated.sequence([
            Animated.timing(headerOpacity, { toValue: 1, duration: 300, useNativeDriver: true }),
            Animated.parallel([
                Animated.timing(listOpacity, { toValue: 1, duration: 400, useNativeDriver: true }),
                Animated.timing(listTranslateY, { toValue: 0, duration: 400, useNativeDriver: true }),
            ]),
        ]).start();
    }, []);

    return (
        <SafeAreaView style={s.container}>
            {/* Header */}
            <Animated.View style={[s.header, { opacity: headerOpacity }]}>
                <Text style={s.headerTitle}>HISTORY</Text>
                <View style={s.weekBadge}>
                    <Text style={s.weekText}>{MOCK_WEEK_COUNT} buzzes 💛</Text>
                </View>
            </Animated.View>

            {/* Section label */}
            <Animated.View style={{ opacity: headerOpacity }}>
                <Text style={s.sectionLabel}>THIS WEEK</Text>
            </Animated.View>

            {/* Buzz list */}
            <Animated.ScrollView
                style={[s.list, { opacity: listOpacity, transform: [{ translateY: listTranslateY }] }]}
                showsVerticalScrollIndicator={false}
            >
                {MOCK_HISTORY.map((item) => (
                    <BuzzEntry
                        key={item.id}
                        direction={item.direction as 'sent' | 'received'}
                        partner={MOCK_PARTNER}
                        timestamp={item.timestamp}
                    />
                ))}
                <View style={{ height: 100 }} />
            </Animated.ScrollView>

            {/* Bottom pill nav — pass through to parent */}
            {/* NOTE: Navigation is handled by the parent TabNavigator in Phase 1 plan 01-04 */}
        </SafeAreaView>
    );
}
```

**BuzzEntry sub-component** (inside the same file, before the main export):
```typescript
function BuzzEntry({
    direction, partner, timestamp
}: {
    direction: 'sent' | 'received';
    partner: string;
    timestamp: string;
}) {
    const isSent = direction === 'sent';
    const label = isSent ? `You buzzed ${partner}` : `${partner} buzzed you`;
    const dotColor = isSent ? Colors.accent : '#888888';

    return (
        <View style={e.row}>
            <View style={[e.dot, { backgroundColor: dotColor }]} />
            <View style={e.content}>
                <Text style={[e.label, isSent && e.sentLabel]}>{label}</Text>
                <Text style={e.timestamp}>{timestamp}</Text>
            </View>
            <View style={[e.arrow, isSent && e.arrowSent]}>
                <Text style={{ color: dotColor, fontFamily: Typography.pixel, fontSize: 10 }}>
                    {isSent ? '→' : '←'}
                </Text>
            </View>
        </View>
    );
}
```

**StyleSheet** (use `StyleSheet.create`):
- `container`: `flex: 1, backgroundColor: Colors.background`
- `header`: `flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 24, paddingTop: 16, paddingBottom: 8`
- `headerTitle`: `fontFamily: Typography.pixel, fontSize: 20, color: Colors.text, letterSpacing: 3`
- `weekBadge`: `backgroundColor: 'rgba(225,255,1,0.1)', borderRadius: 20, paddingHorizontal: 12, paddingVertical: 4, borderWidth: 1, borderColor: 'rgba(225,255,1,0.3)'`
- `weekText`: `fontFamily: Typography.pixel, fontSize: 12, color: Colors.accent`
- `sectionLabel`: `fontFamily: Typography.pixel, fontSize: 11, color: '#555555', letterSpacing: 2, paddingHorizontal: 24, paddingBottom: 8`
- `list`: `flex: 1`
- BuzzEntry `row`: `flexDirection: 'row', alignItems: 'center', paddingHorizontal: 24, paddingVertical: 14, borderBottomWidth: 1, borderBottomColor: 'rgba(255,255,255,0.05)'`
- BuzzEntry `dot`: `width: 8, height: 8, borderRadius: 4, marginRight: 14`
- BuzzEntry `content`: `flex: 1`
- BuzzEntry `label`: `fontFamily: Typography.pixel, fontSize: 13, color: '#AAAAAA'`
- BuzzEntry `sentLabel`: `color: Colors.text`
- BuzzEntry `timestamp`: `fontFamily: Typography.pixel, fontSize: 11, color: '#555555', marginTop: 2`
- BuzzEntry `arrow`: `width: 24, alignItems: 'center'`
</action>

<acceptance_criteria>
- File `mobile/src/screens/HistoryScreen.tsx` exists
- File contains `export default function HistoryScreen`
- File contains `MOCK_HISTORY` array with at least 8 entries
- File contains `BuzzEntry` sub-component
- File uses `Colors.background` for container background
- File uses `Colors.accent` for sent buzz indicator color
- File uses `Typography.pixel` (GeistPixel) for all text
- File uses `Animated.Value` for entry animations with `useNativeDriver: true`
- File imports `getResponsiveValue` from `'../utils/responsive'` OR simply uses fixed values if no responsive sizing is needed
</acceptance_criteria>

## Verification

```bash
# File exists
test -f mobile/src/screens/HistoryScreen.tsx && echo "PASS" || echo "FAIL: file missing"

# Default export
grep -n "export default function HistoryScreen" mobile/src/screens/HistoryScreen.tsx

# Mock data
grep -n "MOCK_HISTORY" mobile/src/screens/HistoryScreen.tsx

# GeistPixel everywhere (no hardcoded font strings other than GeistPixel)
grep -n "fontFamily" mobile/src/screens/HistoryScreen.tsx

# No hardcoded colors (all from theme)
grep -n "#[0-9A-Fa-f]\{6\}" mobile/src/screens/HistoryScreen.tsx
```

## Must-Haves

- `HistoryScreen.tsx` created and renders correctly
- Scrollable list of buzz history entries
- "This Week" section header with count badge
- Sent items highlighted in accent color (`#E1FF01`)
- Received items in muted color (`#888888`)
- Entry fade+slide animation on mount
- `GeistPixel` for ALL text nodes
